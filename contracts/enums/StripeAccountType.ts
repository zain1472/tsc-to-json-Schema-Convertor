export enum AccountType {
  STANDARD = 'standard',
  EXPRESS = 'express',
  CUSTOM = 'custom',
}
export enum BusinessType {
  INDIVIDUAL = 'individual',
  COMPANY = 'company',
}

export enum STATUSES {
  PENDING = 'pending',
  FAILED = 'failed',
  COMPLETED = 'completed',
}

export enum PlatformType {
  CCM = 'ccm',
  TRAVELJINNI = 'travel_jinni',
}
