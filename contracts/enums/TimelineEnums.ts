export enum TravelModes {
  driving = 'driving',
  transit = 'transit',
  bicycling = 'bicycling ',
  walking = 'walking ',
}
export enum TimelineEventType {
  restingStartTime = 'restingStartTime',
  product = 'product',
  allDay = 'allDay',
  restingEndTime = 'restingEndTime',
  transportation = 'transportation',
}

export enum TransportType {
  car = 'car',
  cycling = 'cycling',
  walking = 'walking',
}

export enum SecuredEventType {
  custom = 'custom',
  booked = 'booked',
  addedToPlan = 'addedToPlan',
}

export enum EventType {
  custom = 'custom',
  booked = 'booked',
  addedToPlan = 'addedToPlan',
  product = 'product',
}
