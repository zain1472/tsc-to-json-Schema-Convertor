export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum CustomerActiveStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  WAITING = 'waiting_list',
}

export enum UserType {
  ADMIN = 'admin',
  VENDOR = 'vendor',
  CUSTOMER = 'customer',
}
export enum VendorRole {
  ADMIN = 'admin',
  PRIMARY = 'primary',
}

export enum AuthMedium {
  TRAVEL_JINNI = 'traveljinni',
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
}

export enum SocialAuthMedium {
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
}
