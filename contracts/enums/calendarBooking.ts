export enum DOWNLOADFORMAT {
  CSV = 'csv',
  XLS = 'excel',
}

export enum FREQUENCYSETTING {
  NEVER = 'never',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
}

export enum DAYSETTING {
  YESTERDAY = '1',
  TOMORROW = '2',
}
